//declaração de variavel com o valor inicial: let contador = 0;
//
// condição de parada: contador <= 10;
//
// incrementador: contador = contador + 1
for (let contador = 0; contador <= 10; contador++) {
	//console.log(contador);
}

// Imprima no console numeros de 100 a 1000 porém apenas as centenas:
// ex: 100, 200, 300, 400
//declarar a variavel com seu valor inicial
//colocar a condição de para
// colocar o incrementador

for (let numero = 100; numero <= 1000; numero = numero + 100) {
	//numero+=100
	//console.log(numero);
}
//deu loop infinito? testa ctrl + c para forçar

//Imprima no console apenas os numeros pares de 0 a 10

//declarar a variavel com seu valor inicial
//colocar a condição de parada
// colocar o incrementador
for (let par = 2; par <= 0; par += 2) {
	console.log(par);
}

//Imprima no console o seu nome três vezes

//variavel++ ---> variavel = variavel + 1
//variavel += 1  -----> variavel = variavel + 1
// variavel += 10 ---> variavel = variavel + 10
